import Image from 'next/image';
import Link from 'next/link';
import styles from '../../styles/Home.module.scss';

const Explorer = () => {
    return (
        <nav className={styles.explorer}>
            <span className={styles.explorer__head}>EXPLORER</span>
        </nav>
    )
}

export default Explorer